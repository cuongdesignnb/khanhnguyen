import{readdir,readFile}from'node:fs/promises';import{join,extname}from'node:path';const roots=['app','components','lib','data','prisma'];const bad=/Ãƒ|Ã‚|Ã„|Ã¡Âº|Ã¡Â»|Ã¯Â¿Â½|ï¿½/;let count=0;async function scan(dir:string){for(const entry of await readdir(dir,{withFileTypes:true})){const path=join(dir,entry.name);if(entry.isDirectory())await scan(path);else if(['.ts','.tsx','.js','.jsx','.prisma','.md'].includes(extname(path))){const lines=(await readFile(path,'utf8')).split(/\r?\n/);lines.forEach((line,index)=>{if(bad.test(line)){console.error(`${path}:${index+1}: ${line.trim().slice(0,160)}`);count++}})}}}async function main(){for(const root of roots)await scan(root);if(count){console.error(`Phát hiện ${count} dòng có dấu hiệu mojibake.`);process.exitCode=1}else console.log('Không phát hiện mojibake.')}void main()
