import{NextRequest}from'next/server';import prisma from'@/lib/prisma';import{requireSettingsAdmin}from'@/lib/site-config/admin'
export async function POST(r:NextRequest){const denied=await requireSettingsAdmin(r);if(denied)return denied;const b=await r.json();if(!b.sectionId)return Response.json({success:false,error:'Thiếu khu vực'},{status:400});const data=await prisma.pageSectionItem.create({data:{sectionId:b.sectionId,itemType:b.itemType||'default',title:b.title,subtitle:b.subtitle,description:b.description,content:b.content,value:b.value,suffix:b.suffix,icon:b.icon,imageId:b.imageId,buttonLabel:b.buttonLabel,buttonUrl:b.buttonUrl,secondaryLabel:b.secondaryLabel,secondaryUrl:b.secondaryUrl,isEnabled:b.isEnabled??true,sortOrder:Number(b.sortOrder)||0}});return Response.json({success:true,data})}
